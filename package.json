{
  "name": "prenada",
  "version": "1.0.0",
  "description": "n/a",
  "main": "index.js",
  "repository": "https://github.com/dlion168/prenada",
  "author": "Evan Lin",
  "license": "MIT",
  "private": null,
  "scripts": {
    "start": "cd frontend && yarn start",
    "server": "cd backend && yarn server",
    "install:all": "cd frontend && yarn install --frozen-lockfile && cd ../backend && yarn install --frozen-lockfile",
    "install:prod": "cd frontend && yarn install --freeze-lockfile && cd ../backend && yarn install --freeze-lockfile",
    "build": "cd frontend && yarn build",
    "deploy": "cd backend && yarn deploy"
  },
  "dependencies": {}
}
